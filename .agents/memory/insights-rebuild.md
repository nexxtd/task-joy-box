- [Task Joy Box — Insights rebuild](insights-rebuild.md) — Insights page is now a 14-widget draggable grid (6 free / 6 premium / 2 pro) built on the shared hook src/hooks/useWidgetGrid.ts; widget bodies in src/components/insights/InsightWidgets.tsx (free) + InsightPremiumWidgets.tsx (premium/pro); pure data builders in src/components/insights/insightData.ts recalc live from useBoardContext. Customize Insights panel mirrors Dashboard. AI widgets call POST /api/ai/pro/dashboard-widgets (Pro-gated) — score falls back to buildAiScoreFallback client-side on failure. All native <select> tags replaced with Radix Select from src/components/ui/select.tsx.
